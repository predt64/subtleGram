{
  "name": "subtlegram",
  "version": "1.0.0",
  "description": "Interactive subtitle analyzer for English language learning",
  "main": "index.js",
  "workspaces": [
    "front",
    "back"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=front\" \"npm run dev --workspace=back\"",
    "build": "npm run build --workspace=front && npm run build --workspace=back",
    "install:all": "npm install && npm install --workspace=front && npm install --workspace=back"
  },
  "keywords": [
    "subtitles",
    "analysis",
    "english-learning",
    "nuxt",
    "vue",
    "ai"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "dotenv": "^17.2.2"
  }
}
