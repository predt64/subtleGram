/**
 * Утилита для работы с предложениями
 *
 * @deprecated - больше не используется после упрощения API
 * Пользователь теперь сам передает sentenceText и context
 */

// Файл оставляем пустым для обратной совместимости
// Все функции удалены, так как теперь пользователь сам определяет предложения
